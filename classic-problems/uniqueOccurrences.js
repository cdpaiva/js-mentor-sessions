/**
 * Given an array of numbers, return a boolean to indicate if the number of
 * counts each element of the array is unique.
 *
 * Example:
 *
 * [1, 3, 3, 5, 5, 5]
 *
 * Returns: true => 1 shows 1 time, 3 shows 2 times and 5 shows 3 times
 * All frequencies are unique
 *
 * [1, 2, 2, 3, 3, 55, 55]
 *
 * Returns: false => numbers 2, 3, and 55 all have the same frequency.
 */
