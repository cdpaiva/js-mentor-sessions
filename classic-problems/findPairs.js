/**
 * Given a value, find all pairs in an array of numbers that add up to that
 * value.
 * Each element can be used only once.
 *
 * Example:
 *
 * sum = 10, arr = [5, 5, 8, 1, 3, 2]
 *
 * Returns:
 *
 * [[5, 5], [8, 2]]
 *
 * sum = 4, arr = [2, 2, 2, 2]
 *
 * Returns:
 *
 * [[2, 2], [2, 2]]
 */
