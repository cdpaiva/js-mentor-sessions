/**
 * Given an array of positive numbers, return the first number that is duplicated.
 * Return -1 if no duplicates are found.
 *
 * Example:
 *
 * arr = [5, 7, 8, 1, 7, 3, 5]
 *
 * Returns:
 *
 * 7
 */
